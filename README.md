```
Convert a text file from one (supposedly) japanese encoding to UTF-8

Usage: mojibake-fix [OPTIONS] --dir <DIR>

Options:
  -d, --dir <DIR>  directory path
  -w, --write      whether to write the result to a new file as "_filename"
  -h, --help       Print help
  -V, --version    Print version
```
